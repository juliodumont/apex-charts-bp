import './Header.scss';

const Header = () => {
  return (
    <header className="main-header-container">
      <h1 className="main-header-title">Header</h1>
    </header>
  );
};

export default Header;
